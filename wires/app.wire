
json(null, "resources.states.boot")

multi("Component", "cmp.Webcam")
multi("Component", "cmp.Wait")
multi("Component", "cmp.Sprite")

implements("App", "Main")
implements("GameState", "states.State")

get("App")